---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [
  "src-tauri/Cargo.toml",
  "src-tauri/tauri.conf.json",
  "src-tauri/src/main.rs",
  "src-tauri/capabilities/default.json",
  "package.json",
  "svelte.config.js",
  "vite.config.ts",
  "tsconfig.json",
  "src/app.html",
  "src/routes/+layout.svelte",
  "src/routes/+page.svelte",
  "src/app.css",
  "index.html"
]
autonomous: true
requirements: []
must_haves:
  truths:
    - "App launches without errors"
    - "Window shows 'Spoky' in title bar"
    - "Frontend and backend communicate via Tauri IPC"
  artifacts:
    - path: "src-tauri/Cargo.toml"
      provides: "Tauri dependencies and metadata"
      contains: ["tauri", "tauri-plugin-sql"]
    - path: "src-tauri/tauri.conf.json"
      provides: "Tauri configuration"
      contains: ["identifier", "windows"]
    - path: "src-tauri/src/main.rs"
      provides: "Rust entry point"
      exports: ["main"]
    - path: "package.json"
      provides: "Node dependencies"
      contains: ["@tauri-apps/api", "svelte"]
    - path: "src/routes/+page.svelte"
      provides: "Main page component"
      contains: ["Spoky"]
  key_links:
    - from: "src/routes/+page.svelte"
      to: "src-tauri/src/main.rs"
      via: "Tauri IPC invoke"
      pattern: "invoke\("
---

<objective>
Initialize Tauri 2.0 project with Svelte frontend and verify IPC communication works.

Purpose: Establish the foundational app shell that all subsequent phases build on. Without a working Tauri + Svelte setup, no UI or protocol integration can proceed.

Output: Runnable desktop app with "Hello Spoky" UI that successfully calls a test Tauri command.
</objective>

<execution_context>
@/home/c/.config/opencode/get-shit-done/workflows/execute-plan.md
@/home/c/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/research/SUMMARY.md

## User Decisions (LOCKED)
- **Project Structure**: Standard Tauri 2.0 layout with Svelte in sibling folder (not inside src-tauri)
- **Frontend**: Svelte 5.25+ in `/src` directory at project root
- **Backend**: Rust core in `/src-tauri`
- **Single Cargo.toml**: No workspace/monorepo for v1

## Key Constraints
- Use @tauri-apps/plugin-sql for SQLite (to be configured in Plan 02)
- Tauri 2.0+ with proper capability-based permissions
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Tauri project structure</name>
  <files>
    src-tauri/Cargo.toml
    src-tauri/tauri.conf.json
    src-tauri/src/main.rs
    src-tauri/capabilities/default.json
    src-tauri/icons/
    package.json
    svelte.config.js
    vite.config.ts
    tsconfig.json
    src/app.html
    src/routes/+layout.svelte
    src/routes/+page.svelte
    src/app.css
    index.html
  </files>
  <action>
    Initialize a complete Tauri 2.0 + Svelte project:

    1. Create src-tauri/ directory with Cargo.toml:
       - Set package name to "spoky"
       - Version "0.1.0"
       - Add tauri = "2.0" and tauri-plugin-sql = "2.0" dependencies
       - Configure build script for tauri-build

    2. Create src-tauri/tauri.conf.json:
       - Set app name "Spoky"
       - Set identifier "com.spoky.app"
       - Configure window with title "Spoky", size 1200x800
       - Set frontendDist to "../dist"
       - Set devUrl to "http://localhost:5173"
       - Include sql:default capability for database plugin

    3. Create src-tauri/src/main.rs:
       - Standard Tauri main function
       - Add .plugin(tauri_plugin_sql::Builder::new().build()) for Plan 02
       - Register a simple "greet" command for IPC verification

    4. Create src-tauri/capabilities/default.json:
       - Grant sql:default permission (for @tauri-apps/plugin-sql)
       - Grant core:default permissions

    5. Create package.json at project root:
       - Add @tauri-apps/api, @tauri-apps/cli as dev dependencies
       - Add svelte, @sveltejs/kit, vite as dependencies
       - Configure scripts: dev, build, tauri dev, tauri build

    6. Create SvelteKit configuration files:
       - svelte.config.js with adapter-static (for Tauri)
       - vite.config.ts with tauri plugin
       - tsconfig.json for TypeScript
       - src/app.html template

    7. Create basic Svelte app structure:
       - src/app.css with Tailwind-style reset (Tailwind to be added in Plan 03)
       - src/routes/+layout.svelte as root layout
       - src/routes/+page.svelte with "Hello Spoky" heading
       - Add IPC test: call invoke('greet', { name: 'Spoky' }) and display result

    8. Create index.html for Vite dev server

    IMPORTANT: Follow Tauri 2.0 conventions exactly. Use proper capability-based security model.
  </action>
  <verify>
    <automated>cd /home/c/Dev/Spoky && cargo check --manifest-path src-tauri/Cargo.toml 2>&1 | grep -E "(Compiling|Finished|error)"</automated>
  </verify>
  <done>
    - src-tauri/Cargo.toml exists with tauri and tauri-plugin-sql dependencies
    - src-tauri/tauri.conf.json exists with proper app configuration
    - src-tauri/src/main.rs compiles without errors
    - package.json exists with all required dependencies
    - src/routes/+page.svelte exists and imports @tauri-apps/api
  </done>
</task>

<task type="auto">
  <name>Task 2: Verify build and IPC communication</name>
  <files>
    src-tauri/src/main.rs
    src/routes/+page.svelte
  </files>
  <action>
    Verify the Tauri setup works end-to-end:

    1. Add a test command to src-tauri/src/main.rs:
       ```rust
       #[tauri::command]
       fn greet(name: &str) -> String {
           format!("Hello, {}! Tauri IPC is working.", name)
       }
       ```
       And register it in the main function's tauri::Builder.

    2. Update src/routes/+page.svelte to test IPC:
       - Import { invoke } from '@tauri-apps/api/core'
       - Add a button that calls invoke('greet', { name: 'Spoky' })
       - Display the result on the page
       - Add basic styling to confirm UI renders

    3. Verify Rust compilation: cargo check in src-tauri directory

    4. Verify frontend dependencies: npm install completes without errors

    5. Do NOT run tauri dev (requires user interaction), but verify:
       - All source files are syntactically correct
       - Import paths are correct
       - No missing dependencies

    6. Create a simple build verification script that checks:
       - All required files exist
       - Cargo.toml is valid TOML
       - package.json is valid JSON
       - No syntax errors in main.rs
  </action>
  <verify>
    <automated>cd /home/c/Dev/Spoky && test -f src-tauri/src/main.rs && test -f src/routes/+page.svelte && test -f package.json && echo "All files present" && grep -q "greet" src-tauri/src/main.rs && grep -q "invoke" src/routes/+page.svelte && echo "IPC code present"</automated>
  </verify>
  <done>
    - greet command exists in main.rs and is registered with tauri::Builder
    - +page.svelte imports invoke and calls greet command
    - Result display shows "Hello, Spoky! Tauri IPC is working."
    - cargo check passes without errors
    - npm install can complete (package.json is valid)
  </done>
</task>

</tasks>

<verification>
**Build Verification:**
- `cargo check` in src-tauri/ returns success
- `npm install` at project root completes
- All required files exist with correct content

**IPC Verification:**
- greet command defined in Rust
- invoke call present in Svelte
- Result displayed in UI
</verification>

<success_criteria>
1. src-tauri/ directory exists with valid Cargo.toml and tauri.conf.json
2. src/ directory exists with SvelteKit app structure
3. package.json exists with Tauri and Svelte dependencies
4. greet command works via Tauri IPC (verified by code inspection)
5. App can be launched with `npm run tauri dev` (user action)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
